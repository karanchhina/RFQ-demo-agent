# Required for notebook model calls and integration characterization tests.
OPENAI_API_KEY=

# Required for online traces, dataset sync, and experiments.
LANGSMITH_TRACING=true
LANGSMITH_API_KEY=
LANGSMITH_PROJECT=rfq-demo-agent

# Only needed when a LangSmith key belongs to more than one workspace.
LANGSMITH_WORKSPACE_ID=
